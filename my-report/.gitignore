__pycache__/
*.pyc
.streamlit/secrets.toml

# 2026-09-19: Streamlit Cloud 배포를 위해 data/*.csv를 저장소에 포함시키기로
# 결정(사용자 확인) — 배포된 앱이 원본 없이도 그대로 돌아가려면 필요하다.
# 이전엔 "재생성 가능한 파생 데이터"라는 이유로 뺐었다(합성데이터/generated_2year
# 에서 다시 만들 수 있음은 여전히 사실이다 — 그래서 원본 생성 스크립트는
# 계속 이 저장소 밖에 둔다).

# 게이트3에서 만드는 PDF는 auto/human 절 + fpdf2로 언제든 다시 만들 수 있는
# 산출물이라 저장소에 안 넣는다(발송 이력 자체는 runs/*.json에 이미 남는다).
/runs/*.pdf
